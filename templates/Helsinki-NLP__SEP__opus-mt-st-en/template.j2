{% set repo_id = repo_id | default("Helsinki-NLP/opus-mt-st-en") %}
{% set device = device | default("cuda:0") %}
{% set input_texts = input_texts | default(["This is a test."]) %}
{% set max_length = max_length | default(512) %}
{% set num_beams = num_beams | default(6) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
input_texts = {{ input_texts }}
max_length = {{ max_length }}
num_beams = {{ num_beams }}

import os
import json
import torch
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id).to(device)

inputs = tokenizer(input_texts, return_tensors="pt", padding=True).to(device)
generated_ids = model.generate(**inputs, max_length=max_length, num_beams=num_beams)
translations = tokenizer.batch_decode(generated_ids, skip_special_tokens=True)

RESULT = {"translations": translations}
