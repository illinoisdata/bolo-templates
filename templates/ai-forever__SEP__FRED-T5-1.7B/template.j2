{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("ai-forever/FRED-T5-1.7B") %}
{% set prompt = prompt | default("<LM>Принялся Кутузов рассказывать свою историю как он сюда попал. Началось") %}
{% set max_length = max_length | default(256) %}
{% set num_return_sequences = num_return_sequences | default(1) %}
{% set do_sample = do_sample | default(False) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
prompt = """{{ prompt }}"""
max_length = {{ max_length }}
num_return_sequences = {{ num_return_sequences }}
do_sample = {{ do_sample }}

import os
from pathlib import Path

import torch
from transformers import AutoModelForSeq2SeqLM, AutoTokenizer

torch_device = torch.device(device)

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id)
model.to(torch_device)

encoded_inputs = tokenizer(prompt, return_tensors="pt")
input_ids = encoded_inputs["input_ids"].to(torch_device)
attention_mask = encoded_inputs["attention_mask"].to(torch_device)

generated_tokens = model.generate(
    input_ids=input_ids,
    attention_mask=attention_mask,
    max_length=max_length,
    num_return_sequences=num_return_sequences,
    do_sample=do_sample,
)

generated_texts = tokenizer.batch_decode(generated_tokens, skip_special_tokens=True)

RESULT = {"generated_texts": generated_texts}
