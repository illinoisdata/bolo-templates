{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("alibabasglab/mossformer2-librimix-2spk") %}
import subprocess
subprocess.run(["ls", "-R", "MossFormer2"], check=True)
