{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("ybelkada/tiny-random-T5ForConditionalGeneration-calibrated") %}
{% set prompt = prompt | default("translate English to German: The house is wonderful.") %}
{% set max_new_tokens = max_new_tokens | default(32) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
prompt = "{{ prompt }}"
max_new_tokens = {{ max_new_tokens }}

from transformers import AutoModelForSeq2SeqLM, AutoTokenizer
import os
import torch

model = AutoModelForSeq2SeqLM.from_pretrained(repo_id)
model = model.to(device)
tokenizer = AutoTokenizer.from_pretrained(repo_id)

inputs = tokenizer(prompt, return_tensors="pt")
inputs = {k: v.to(device) for k, v in inputs.items()}
outputs = model.generate(**inputs, max_new_tokens=max_new_tokens)
translations = tokenizer.batch_decode(outputs, skip_special_tokens=True)

RESULT = {"translations": translations}
