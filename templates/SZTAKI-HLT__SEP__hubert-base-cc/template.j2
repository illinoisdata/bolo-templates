{% set repo_id = repo_id %}
{% set device = device %}
{% set text_input = "Ez egy példamondat a modellhez." %}
{% set output_dir = "/u/yli77/work_link/yli77/templates/milestone1/transformers/others/_mini-swe/SZTAKI-HLT__SEP__hubert-base-cc" %}

repo_id = "{{ repo_id }}"
device = "{{ device }}"
text_input = "{{ text_input }}"
output_dir = "{{ output_dir }}"

import torch
from pathlib import Path
from transformers import AutoModel, AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModel.from_pretrained(repo_id).to(device)
model.eval()

encoded_inputs = tokenizer(
    text_input,
    return_tensors="pt",
    padding="max_length",
    truncation=True,
    max_length=512,
)
encoded_inputs = {k: v.to(device) for k, v in encoded_inputs.items()}

with torch.no_grad():
    outputs = model(**encoded_inputs)

last_hidden_state = outputs.last_hidden_state.cpu()
RESULT = {"last_hidden_state": last_hidden_state}

if __name__ == "__main__":
    out_path = Path(output_dir)
    out_path.mkdir(parents=True, exist_ok=True)
    torch.save(RESULT["last_hidden_state"], out_path / "last_hidden_state.pt")