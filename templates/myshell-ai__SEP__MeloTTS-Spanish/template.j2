{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("myshell-ai/MeloTTS-Spanish") %}
import transformers
print("vits" in dir(transformers.models))
print(getattr(transformers.models, "vits", None))
