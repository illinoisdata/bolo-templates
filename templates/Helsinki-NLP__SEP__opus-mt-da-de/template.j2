{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("Helsinki-NLP/opus-mt-da-de") %}
{% set input_text = input_text | default("Dette er en test på dansk for maskinoversættelse til tysk.") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
input_text = "{{ input_text }}"

import json
from pathlib import Path

import torch
from transformers import AutoModelForSeq2SeqLM, MarianTokenizer

torch_device = torch.device(device)

tokenizer = MarianTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id)
model.to(torch_device)
model.eval()

input_texts = [input_text]
inputs = tokenizer(input_texts, return_tensors="pt", padding=True, truncation=True)
inputs = {key: value.to(torch_device) for key, value in inputs.items()}

generated_ids = model.generate(**inputs)
translations = tokenizer.batch_decode(generated_ids, skip_special_tokens=True)

RESULT = {"translations": translations}
