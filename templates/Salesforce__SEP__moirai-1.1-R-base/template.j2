{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("Salesforce/moirai-1.1-R-base") %}
repo_id = "{{ repo_id }}"

import json
from pathlib import Path
from huggingface_hub import list_repo_files

files = list_repo_files(repo_id)
RESULT = {"files": files}
