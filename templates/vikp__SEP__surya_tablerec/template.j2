{% set repo_id = repo_id | default("vikp/surya_tablerec") %}
{% set device = device | default("cuda:0") %}
{% set image = image | default("/u/yli77/projects/ML-code-generation/data/doc.jpeg") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
image = "{{ image }}"

import inspect
import transformers

module = transformers.models
print([name for name in dir(module) if name.endswith("table_rec")])
print(dir(transformers.models.donut))
print(transformers.__version__)
