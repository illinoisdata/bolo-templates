{% set repo_id = "vikp/surya_tablerec" %}
{% set device = "cuda:0" %}
{% set image = "/u/yli77/projects/ML-code-generation/data/doc.jpeg" %}
{% set output_dir = "/u/yli77/work_link/yli77/templates/milestone1/transformers/others/_mini-swe/vikp__SEP__surya_tablerec" %}

repo_id = "{{ repo_id }}"
device = "{{ device }}"
image = "{{ image }}"
output_dir = "{{ output_dir }}"

import inspect
import transformers

module = transformers.models
print([name for name in dir(module) if name.endswith("table_rec")])
print(dir(transformers.models.donut))
print(transformers.__version__)