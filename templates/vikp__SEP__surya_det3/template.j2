{% set device = "cuda:0" %}
{% set repo_id = "vikp/surya_det3" %}
repo_id = "{{ repo_id }}"
from huggingface_hub import snapshot_download
import os
snapshot_dir = snapshot_download(repo_id=repo_id)
for root, _, files in os.walk(snapshot_dir):
    for file in files:
        print(os.path.join(root, file))
