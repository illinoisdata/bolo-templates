{% set repo_id = repo_id | default("Felix92/doctr-torch-parseq-multilingual-v1") %}
{% set device = device | default("cuda:0") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"

import doctr
print(dir(doctr))
