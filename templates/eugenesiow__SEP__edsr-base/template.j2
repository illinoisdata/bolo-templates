{% set repo_id = repo_id | default("eugenesiow/edsr-base") %}
{% set device = device | default("cuda:0") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"

import os
import transformers

models_dir = os.path.join(os.path.dirname(transformers.__file__), "models")
models = sorted(os.listdir(models_dir)) if os.path.isdir(models_dir) else []

RESULT = {"models": models}
