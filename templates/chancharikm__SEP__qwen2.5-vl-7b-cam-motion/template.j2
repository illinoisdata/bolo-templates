{% set repo_id = repo_id | default("chancharikm/qwen2.5-vl-7b-cam-motion") %}
{% set device = device | default("cuda:0") %}
{% set video_input_path = video_input_path | default("/u/yli77/projects/ML-code-generation/data/video.avi") %}
{% set question_text = question_text | default("Describe the camera motion in this video.") %}
{% set max_new_tokens = max_new_tokens | default(128) %}
{% set fps = fps | default(8.0) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
video_input_path = "{{ video_input_path }}"
question_text = "{{ question_text }}"
max_new_tokens = {{ max_new_tokens }}
fps = {{ fps }}

import torch
from pathlib import Path

from qwen_vl_utils import process_vision_info
from transformers import AutoProcessor, Qwen2_5_VLForConditionalGeneration

processor = AutoProcessor.from_pretrained("Qwen/Qwen2.5-VL-7B-Instruct")
model = Qwen2_5_VLForConditionalGeneration.from_pretrained(repo_id, torch_dtype=torch.bfloat16)
model = model.to(device)

if video_input_path.startswith("file://") or video_input_path.startswith("http://") or video_input_path.startswith("https://"):
    video_uri = video_input_path
else:
    video_uri = Path(video_input_path).expanduser().resolve().as_uri()

messages = [
    {
        "role": "user",
        "content": [
            {"type": "video", "video": video_uri, "fps": fps},
            {"type": "text", "text": question_text},
        ],
    }
]

text_prompt = processor.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
image_inputs, video_inputs, video_kwargs = process_vision_info(messages, return_video_kwargs=True)
inputs = processor(
    text=[text_prompt],
    images=image_inputs,
    videos=video_inputs,
    padding=True,
    return_tensors="pt",
    **video_kwargs,
)
inputs = inputs.to(device)

with torch.inference_mode():
    outputs = model.generate(
        **inputs,
        max_new_tokens=max_new_tokens,
        do_sample=False,
        return_dict_in_generate=True,
    )

prefix_lengths = inputs.attention_mask.sum(dim=1).tolist()
generated_ids_trimmed = [
    sequence[prefix_len:]
    for sequence, prefix_len in zip(outputs.sequences, prefix_lengths)
]
output_text = processor.batch_decode(
    generated_ids_trimmed,
    skip_special_tokens=True,
    clean_up_tokenization_spaces=False,
)

RESULT = {"generated_text": output_text}
