{% set repo_id = repo_id | default("hubertsiuzdak/snac_24khz") %}
{% set device = device | default("cuda:0") %}
{% set audio_path = audio_path | default("/u/yli77/projects/ML-code-generation/data/audio.wav") %}
{% set sampling_rate = sampling_rate | default(24000) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
audio_path = "{{ audio_path }}"
sampling_rate = {{ sampling_rate }}

import os
import requests
from io import BytesIO
import torch
import torchaudio
from snac import SNAC
import json

model = SNAC.from_pretrained(repo_id).eval().to(device)

if audio_path.startswith("http"):
    response = requests.get(audio_path)
    response.raise_for_status()
    waveform, sr = torchaudio.load(BytesIO(response.content))
else:
    waveform, sr = torchaudio.load(audio_path)

if sr != sampling_rate:
    waveform = torchaudio.functional.resample(waveform, sr, sampling_rate)

waveform = waveform.to(device)
audio_input = waveform.unsqueeze(0)

with torch.inference_mode():
    codes = model.encode(audio_input)
    audio_hat = model.decode(codes)

audio_hat = audio_hat.squeeze(0).cpu()
codes = [code.cpu().numpy().tolist() for code in codes]

RESULT = {"audio_hat": audio_hat, "codes": codes}
