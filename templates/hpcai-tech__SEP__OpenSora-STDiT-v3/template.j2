{% set repo_id = repo_id | default("hpcai-tech/OpenSora-STDiT-v3") %}
{% set device = device | default("cuda:0") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
import pkgutil
import transformers
modules = [m.name for m in pkgutil.iter_modules(transformers.models.__path__) if "stdit" in m.name.lower()]
RESULT = {"modules": modules}
