{% set repo_id = repo_id | default("MingZhong/unieval-dialog") %}
{% set device = device | default("cuda:0") %}
{% set input_text = input_text | default("Conversation: Hello, how are you? Response: I'm doing well.") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
input_text = "{{ input_text }}"

from transformers import AutoTokenizer, AutoModelForSeq2SeqLM
import torch
import os

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id).to(device)

inputs = tokenizer(input_text, return_tensors="pt").to(device)
outputs = model.generate(**inputs)
evaluation = tokenizer.batch_decode(outputs, skip_special_tokens=True)[0]

RESULT = {"evaluation": evaluation}
