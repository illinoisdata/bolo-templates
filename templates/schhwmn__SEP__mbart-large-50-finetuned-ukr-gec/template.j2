{% set repo_id = repo_id | default("schhwmn/mbart-large-50-finetuned-ukr-gec") %}
{% set device = device | default("cuda:0") %}
{% set text = text | default('я й не думав що комп\'ютерна лінгвістика це легкоо.') %}
{% set num_beams = num_beams | default(5) %}
{% set max_length = max_length | default(200) %}
{% set early_stopping = early_stopping | default(true) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
text = "{{ text }}"
num_beams = {{ num_beams }}
max_length = {{ max_length }}
early_stopping = {{ early_stopping }}

import os
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id).to(device)

inputs = tokenizer(text, return_tensors="pt")
inputs = {k: v.to(device) for k, v in inputs.items()}

outputs = model.generate(**inputs, num_beams=num_beams, max_length=max_length, early_stopping=early_stopping)
generated_text = tokenizer.batch_decode(outputs, skip_special_tokens=True)[0]

RESULT = {"generated_text": generated_text}
