{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("Kyle1668/imdb-t5-large") %}
{% set input_text = input_text | default('The movie was fantastic and had a thrilling plot.') %}
{% set prefix = prefix | default('summarize: ') %}
{% set max_length = max_length | default(200) %}
{% set min_length = min_length | default(30) %}
{% set length_penalty = length_penalty | default(2.0) %}
{% set num_beams = num_beams | default(4) %}
{% set no_repeat_ngram_size = no_repeat_ngram_size | default(3) %}
{% set early_stopping = early_stopping | default(True) %}
repo_id = '{{ repo_id }}'
device = '{{ device }}'
input_text = '{{ input_text }}'
prefix = '{{ prefix }}'
max_length = {{ max_length }}
min_length = {{ min_length }}
length_penalty = {{ length_penalty }}
num_beams = {{ num_beams }}
no_repeat_ngram_size = {{ no_repeat_ngram_size }}
early_stopping = {{ early_stopping }}

from transformers import AutoTokenizer, AutoModelForSeq2SeqLM
import torch
import os

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id).to(device)

text = prefix + input_text
inputs = tokenizer(text, return_tensors='pt').to(device)
outputs = model.generate(**inputs, max_length=max_length, min_length=min_length, length_penalty=length_penalty, no_repeat_ngram_size=no_repeat_ngram_size, num_beams=num_beams, early_stopping=early_stopping)
decoded = tokenizer.batch_decode(outputs, skip_special_tokens=True)
summary_text = decoded[0]

RESULT = {'summary_text': summary_text}
