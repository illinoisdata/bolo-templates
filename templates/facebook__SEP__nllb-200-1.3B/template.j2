{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("facebook/nllb-200-1.3B") %}
{% set src_text = src_text | default("Hello world") %}
{% set src_lang = src_lang | default("eng_Latn") %}
{% set tgt_lang = tgt_lang | default("fra_Latn") %}
{% set max_new_tokens = max_new_tokens | default(60) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
src_text = "{{ src_text }}"
src_lang = "{{ src_lang }}"
tgt_lang = "{{ tgt_lang }}"
max_new_tokens = {{ max_new_tokens }}
from pathlib import Path
import sys
from transformers import NllbTokenizer, AutoModelForSeq2SeqLM

tokenizer = NllbTokenizer.from_pretrained(repo_id, src_lang=src_lang, tgt_lang=tgt_lang)
print(tokenizer.__class__)
print(dir(tokenizer))
sys.exit(0)
