{% set repo_id = repo_id | default("Audiogen/agc-discrete") %}
{% set device = device | default("cuda:0") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
import pkgutil
import transformers
modules = sorted(name for _, name, _ in pkgutil.iter_modules(transformers.models.__path__))
RESULT = {"model_modules": modules}
