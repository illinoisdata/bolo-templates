{% set device = device | default("cuda:0") %}
{% set repo_id = repo_id | default("utrobinmv/t5_translate_en_ru_zh_large_1024") %}
{% set input_text = input_text | default("translate to ru: The purpose of the development is to provide users with a personal synchronized interpreter.") %}
{% set generate_max_length = generate_max_length | default(256) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
input_text = "{{ input_text }}"
generate_max_length = {{ generate_max_length }}

from pathlib import Path

import torch
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModelForSeq2SeqLM.from_pretrained(repo_id)
model = model.to(device)

tokenized_inputs = tokenizer(input_text, return_tensors="pt")
tokenized_inputs = {key: value.to(device) for key, value in tokenized_inputs.items()}

generated_tokens = model.generate(**tokenized_inputs, max_length=generate_max_length)
decoded_text = tokenizer.batch_decode(generated_tokens, skip_special_tokens=True)[0]
RESULT = {"translated_text": decoded_text}
