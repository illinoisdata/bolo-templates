{% set repo_id = "datalab-to/texify" %}
repo_id = "{{ repo_id }}"

from huggingface_hub import list_repo_files
files = list_repo_files(repo_id)
print("files in repo:")
for path in files:
    print(path)