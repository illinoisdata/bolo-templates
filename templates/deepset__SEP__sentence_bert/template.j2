{% set repo_id = repo_id | default("deepset/sentence_bert") %}
{% set device = device | default("cuda:0") %}
{% set sentences = sentences | default(["This is an example sentence for embedding.", "Here is another sentence to embed."]) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
sentences = {{ sentences }}

import os
from pathlib import Path
import numpy as np
import torch
from transformers import AutoTokenizer, AutoModel

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModel.from_pretrained(repo_id).to(device)
model.eval()

encoded = tokenizer(sentences, padding=True, truncation=True, return_tensors="pt")
encoded = {k: v.to(device) for k, v in encoded.items()}

with torch.no_grad():
    outputs = model(**encoded)

last_hidden = outputs.last_hidden_state
attention_mask = encoded["attention_mask"]
mask = attention_mask.unsqueeze(-1)
sum_embeddings = (last_hidden * mask).sum(1)
lengths = mask.sum(1)
embeddings = sum_embeddings / lengths
embeddings = embeddings.cpu().numpy()

RESULT = {"embeddings": embeddings}
