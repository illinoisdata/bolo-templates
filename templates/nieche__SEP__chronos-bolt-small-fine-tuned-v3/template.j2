{% set repo_id = repo_id | default("nieche/chronos-bolt-small-fine-tuned-v3") %}
{% set device = device | default("cuda:0") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"

from transformers import T5ForConditionalGeneration
import torch
import os

model = T5ForConditionalGeneration.from_pretrained(repo_id)
model.to(device)
RESULT = {"loaded": True}
