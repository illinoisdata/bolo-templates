{% set repo_id = repo_id | default("google/rembert") %}
{% set device = device | default("cuda:0") %}
{% set text = text | default("This is a sample input text for RemBERT.") %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
text = "{{ text }}"

from transformers import AutoTokenizer, AutoModel
import torch
import numpy as np
import os

tokenizer = AutoTokenizer.from_pretrained(repo_id)
model = AutoModel.from_pretrained(repo_id).to(device)
inputs = tokenizer(text, return_tensors="pt").to(device)
outputs = model(**inputs)

last_hidden_state = outputs.last_hidden_state
pooler_output = getattr(outputs, "pooler_output", None)

last_hidden_state = last_hidden_state.detach().cpu().numpy()
if pooler_output is not None:
    pooler_output = pooler_output.detach().cpu().numpy()

RESULT = {"last_hidden_state": last_hidden_state}
if pooler_output is not None:
    RESULT["pooler_output"] = pooler_output
