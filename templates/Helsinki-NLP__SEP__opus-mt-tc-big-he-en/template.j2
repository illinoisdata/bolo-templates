{% set repo_id = repo_id | default("Helsinki-NLP/opus-mt-tc-big-he-en") %}
{% set device = device | default("cuda:0") %}
{% set src_text = src_text | default(["היא שכחה לכתוב לו.", "אני רוצה לדעת מיד כשמשהו יקרה."]) %}
repo_id = "{{ repo_id }}"
device = "{{ device }}"
src_text = {{ src_text }}

from transformers import MarianTokenizer, MarianMTModel

tokenizer = MarianTokenizer.from_pretrained(repo_id)
model = MarianMTModel.from_pretrained(repo_id).to(device)

inputs = tokenizer(src_text, return_tensors="pt", padding=True)
inputs = {k: v.to(device) for k, v in inputs.items()}

translated = model.generate(**inputs)

translations = [tokenizer.decode(t, skip_special_tokens=True) for t in translated]

RESULT = {"translations": translations}
